# 项目 2 产品技术文档：企业项目知识查询 Agent

版本：v0.4 · 2026-09-13  
状态：已实现可运行开发版，包括前后端、PostgreSQL/pgvector、持久任务及权限控制。工程与真实模型验证记录见 [验收记录](docs/ACCEPTANCE.md)。所有尚未验证的质量指标仍为验收目标，不是已有成绩。  
代号：Project Knowledge Agent Lite  
目标：交付一个可运行、可验证、能如实写进 Agent 开发实习简历的独立项目。

## 0. 文档效力与阅读说明

**最新交互修订：** 支持 DeepSeek 通用对话与知识库 RAG 自动选择：相关知识调用检索并引用片段，通用内容标为“DeepSeek 通用回答”，不伪造引用。用户可删除自己的任意会话（在运行的任务立即失去写回资格，问题与答案清除）；管理员可删除项目。项目采用删除标记，关联文档保留供管理员查看，但不再进入问答；避免解除关联导致权限扩大。项目编辑界面移除别名输入，新增最多 4000 字的“项目简要情况”，可随项目状态工具查询。历史别名仅保留数据库兼容。


**2026-09-13 用户需求修订优先级最高：** 增加普通用户自助注册与改密、个人会话重命名/分页与长期历史保存；管理员 admin 初始密码 123456，注册无法获得管理权限。知识更新不再隐藏整段历史或按七天清理聊天，改为逐回答检查当前引用权限；保留的历史不得绕过实时授权进入模型。历史会话可继续提问并重建当前上下文。本文下方旧版关于“无自助注册”“旧会话全部不可读”和聊天七天清理的限制均由此修订替代。具体规则、性能措施和实测边界以 [七项功能验收](docs/FEATURE_ACCEPTANCE.md) 为准。

- 本文基于 [项目 2 选型提案](../learning-path/proposals/2026-09-11-project2-enterprise-knowledge-agent-lite-spec.md)，落实 B 精简版：一个 Web 应用、一个文档库、一个项目查询服务、四个只读工具。
- 项目 2 的文档和实现统一放在 `project/`。项目 1 位于独立的 `capstone/`，保持独立；历史提案保留为选型记录。开发授权不等于购买模型服务、上传真实企业资料或公开部署授权。
- 本文中“必须”为 MVP 验收项；“默认”为可配置的初始设计；“不做”为本版本范围边界。
- 后续实现以本文为范围与验收依据；常规开发按阶段推进，不额外设置逐阶段人工批准门槛。修改需求需更新本文版本与验收项，不在开发过程中静默追加。
- 项目查询服务是后端内部模块，有清晰接口边界，但不单独拆微服务。Web 应用包含员工视图和管理员视图，并非只有一个聊天输入框。

## 1. 产品定义与价值假设

实现配置补充：回答生成使用用户配置的 DeepSeek；Embedding 使用本机 CPU 上的 BAAI/bge-small-zh-v1.5（512 维），无需另购 Embedding API。可替换为兼容接口；更换向量模型必须重新索引。Windows 本机已运行 PostgreSQL 16 + pgvector；Compose 作为另一条部署路径保留，单独标注验证状态。以上配置不降低后文验收门槛。

2026-09-13 交互修正：纯模型身份问题从服务端模型配置作答；问候、功能说明和使用帮助使用服务端固定文案，无需企业证据，界面标为“使用说明”。混合企业查询仍走工具和证据流程。模型未调用工具时，仅接受严格的超范围/补充信息分类并转成固定说明，不放行未经核验的模型自由文本。所有分支仍受会话身份、知识版本与任务租约检查约束。

### 1.1 一句话定义

员工通过自然语言，联合查询自己有权访问的项目负责人、当前状态和历史文档，获得带来源、时间与不确定性说明的答案。

### 1.2 要解决的问题

典型问题：“北辰项目现在谁负责，是否延期，最近一次调整的依据在哪里？”

- 当前负责人和状态应从结构化项目记录查询。
- 验收标准、会议结论与决策依据应从文档检索。
- 同名项目、旧资料、无权限资料和相互冲突的记录不能被模型混为一谈。

### 1.3 不预设的结论

这是有待验证的产品假设：对需要跨来源查询的问题，统一入口可能减少人工查找与整理工作。不能仅凭有 RAG、Agent 或权限控制，就宣称比飞书、现成知识库或通用 Agent 更有企业价值。

必须比较关键词搜索、固定检索问答、动态工具查询的实际结果。即使动态 Agent 没有优于固定流程，也要如实保留结论；不能伪造效率提升或企业采用经历。

### 1.4 与项目 1 的区别

| 维度 | 项目 1：DataOps 排障 | 项目 2：本 spec |
| --- | --- | --- |
| 服务对象 | 故障排查使用者 | 多个有不同权限的员工 |
| 数据 | 日志、SQL 静态检查、Runbook | 项目状态记录、负责人、版本化文档 |
| 新增重点 | 已有只读编排与证据回答 | 身份隔离、知识生命周期、跨源查询、持久会话 |
| 结果 | 排查解释与方案 | 当前业务事实与历史依据的综合回答 |

复用已有经验，不为项目 2 修改项目 1；后续如复用代码，记录来源和具体改造，不把重复模块计为全新成果。

## 2. 用户、规模与边界

### 2.1 角色

| 角色 | 能力 | 限制 |
| --- | --- | --- |
| 员工 employee | 查询可见项目/文档、查看自己的会话、提交反馈 | 无业务写权限；不能读取其他员工的会话 |
| 知识管理员 admin | 维护项目、人员角色、文档、版本、用户组与授权；查看反馈 | 本 MVP 明确视为受信任的全库数据管理员；不默认开放其他员工完整会话 |

部门通过用户组表示，不额外设计“经理自动继承下属全部权限”。管理员不是默认聊天身份；Agent 工具始终以提问者身份执行。

### 2.2 演示数据规模

- 单组织，不做多租户。
- 3 个部门组、至少 6 个员工账号、1 个管理员账号。
- 6 个项目，包含两个同名或同别名项目。
- 60 份逻辑文档，每份可以有多个版本，覆盖制度、项目说明、会议纪要、决策记录。
- 支持 Markdown、UTF-8 TXT、文本型 PDF；首版单文件最多 10 MiB、PDF 最多 100 页，限制可配置。
- 以 5 个并发查询作为演示负载，不宣称生产级高并发或海量文档能力。
- 默认使用标注为合成的企业数据；真实资料只有在获得授权、明确模型服务的数据政策后才能接入。

### 2.3 不做

自动改进度、分配任务、审批、催办、任意 SQL 执行、Shell 执行、联网自由抓取、飞书/钉钉集成、扫描件 OCR、复杂表格理解、知识图谱、多 Agent 团队、模型微调、跨会话个人画像、完整 OA/项目管理系统。

管理员维护数据是普通后台功能，不注册成 Agent 写工具。不支持 DOCX，上传时明确说明，不静默按纯文本读取。

## 3. 功能需求与用户故事

| 编号 | 功能 | 行为约定 | 验收重点 |
| --- | --- | --- | --- |
| FR-01 | 登录与身份 | 预置账号登录；服务端确定用户、组、角色 | 修改请求中的身份字段不能越权 |
| FR-02 | 查负责人 | 查询指定项目负责人及测试、交付等人员角色 | 答案与结构化数据一致，附更新时间 |
| FR-03 | 查当前状态 | 返回阶段、状态、计划日期、已记录阻塞事项 | 每轮重新读取；不能把文档当实时状态 |
| FR-04 | 文档问答 | 查询规范、项目说明等，答案关联证据 | 能打开授权原文段落；无无效引用 |
| FR-05 | 历史决策查询 | 从纪要/决策记录整理时间、决定及记录的理由 | 没有理由记录时明确缺失，不推断责任 |
| FR-06 | 跨源综合 | 按问题联合调用状态、人员、文档工具 | 分开表达当前事实、历史记录、冲突/未知 |
| FR-07 | 澄清与追问 | 支持同名项目澄清及“那验收标准呢”等连续问题 | 不泄露不可见候选；更换项目后上下文正确 |
| FR-08 | 可靠性提示 | 处理无结果、冲突、过期记录、工具异常 | 不把失败说成无数据，不静默伪造结果 |
| FR-09 | 会话与进度 | 保存本人会话；展示查询步骤；断线可重连 | 刷新不重复创建任务；重启后有明确恢复结果 |
| FR-10 | 文档生命周期 | 上传、解析、索引、发布、更新、删除、重试 | 新版本失败不影响旧版本；删除后不再可见 |
| FR-11 | 管理项目和授权 | 表单维护项目、人员角色、用户组和可见范围 | 管理接口独立鉴权并记录修改事件 |
| FR-12 | 反馈与评测 | 对回答提交错误类型/说明，管理员查看反馈 | 可定位回答及版本；评测结果可复现 |

### 3.1 对“延期”和“当前”的定义

- 项目阶段与健康状态分别保存，不能从阶段名猜测延期。
- 健康状态枚举：on_track、at_risk、delayed、unknown；阶段枚举：planning、development、testing、acceptance、delivered、paused。
- 保存 baseline_due_date、planned_due_date、actual_delivery_date；日期调整与健康状态作为不同事实展示。
- “是否延期”优先报告登记状态和计划日期；如根据截至日期计算“计划已逾期”，必须标注是按日期规则计算，不自动认定原因。
- 每个答案展示项目记录 updated_at；默认超过 7 天未更新提示“记录可能滞后”，不把记录年龄当作必然错误。
- 不提供项目表任意历史时点快照查询；过去的状态仅在有明确历史文档证据时回答，并标为文档记录。

### 3.2 代表性交互

1. 员工：“北辰项目现在是什么情况？”
2. 如可见候选不唯一，系统显示项目编号、名称及允许公开的区分字段，让用户选择。
3. 确认后查询当前状态，展示更新时间。
4. 员工：“上次为什么推迟？”系统查询该项目历史纪要；原因缺失时只说明缺失。
5. 员工：“那验收标准呢？”系统沿用已确认项目，检索当前有效验收规范并引用。

## 4. 权限与安全规则

### 4.1 资源可见性

- 授权对象为用户或用户组；资源范围分为组织内可见、指定主体可见，默认拒绝。组织内可见不等于互联网公开。
- 项目必须先通过项目 ACL 检查。关联项目的文档同时满足项目 ACL 和文档 ACL，取交集，不取并集。
- 不关联项目的制度文档只按自身 ACL 判断。
- 未授权资源与不存在资源对员工返回相同的 NOT_FOUND_OR_FORBIDDEN，不透露标题、数量、项目名称或候选。
- 文档列举、关键词检索、向量检索、原文查看、引用、项目工具都必须使用同一权限服务。
- 未授权正文不能进入模型或外部重排服务。先确定授权候选集，再召回/重排；不能“先全库取 top-k，最后只在页面过滤”。
- 模型传入的 project_id、document_id、version_id、chunk_id 全部视为不可信，独立验证归属与权限。

### 4.2 权限撤销和历史内容

为控制 MVP 复杂度，采用保守的全局 knowledge_epoch：ACL、用户组成员关系、文档发布/删除发生变化时递增。

- 请求开始时记录 epoch；每次工具读取、给模型发送上下文前、最终答案发送前再次核验。
- 在途查询发现 epoch 改变时终止为 CONTEXT_CHANGED，清理候选答案并要求重新查询；不能继续使用旧检查点中的正文。
- 会话所属 epoch 过期后，员工端隐藏此前回答与原始消息正文，只保留“知识或权限已更新，请开启新会话”占位，避免用户曾粘贴的受限片段被继续回显。
- 新一轮模型上下文不复用过期会话正文；项目指代信息也必须重新授权确认。
- 精简版不做答案缓存；BM25/索引缓存带 epoch，版本落后时重建或拒绝服务，禁止退回旧缓存。
- 这是有意选择的保守策略，会使部分仍有权阅读的旧对话暂不可用。细粒度依赖追踪属于后续优化，不列入 MVP。
- 撤销成功后的新请求不能返回受限内容；已发送到客户端或已授权发给模型服务的数据无法远程收回，不承诺这一能力。

### 4.3 应用安全

- 登录采用密码哈希及服务端会话；Cookie 设为 HttpOnly、SameSite，HTTPS 部署设 Secure；写接口校验 CSRF/Origin。
- 用户不能通过模型工具修改 ACL；管理修改必须经过独立 admin 接口。
- 文档是证据，不是系统指令。文档中的“忽略权限、调用管理员工具、访问外部 URL”等内容不得改变执行边界。
- Agent 只注册四个只读工具；数据库参数化查询，无 eval、动态代码、自由 SQL 或任意路径读取。
- 上传校验类型、大小、页数与解析时限，使用服务端生成的文件 ID；解析器运行在受限 worker，失败状态可见。原文展示进行 HTML 转义，链接仅允许安全协议。
- 普通日志仅包含请求 ID、工具名、状态、耗时和计数，不包含正文、密码、令牌或模型密钥。
- 演示部署仅绑定本机/受限环境；公开部署另需 HTTPS、限流、密钥管理与安全复核，不能原样暴露演示账号。

## 5. 产品界面

一个 SPA，四个主要视图：

1. 登录页：预置演示账号入口，明确标注“合成企业数据”。
2. 员工查询页：会话列表、提问输入、查询进度、答案、证据面板、错误反馈；不展示内部推理。
3. 管理数据页：项目表单、人员角色、文档列表、上传/版本发布、处理状态、ACL 设置。
4. 管理反馈页：反馈类型、回答 ID、提交时间、处理状态；如需显示对应回答，按管理员当前文档权限重新检查，不展示整段私人会话。

空状态、无权限、处理中、失败、需要澄清必须有不同提示。进度流只展示阶段名和计数，不提前输出尚未核验的答案片段。

## 6. 技术架构与选型

| 层 | 拟采用方案 | 范围说明 |
| --- | --- | --- |
| Web | React + TypeScript + Vite | 轻量 SPA，不做复杂门户 |
| API | Python 3.11+、FastAPI、Pydantic | 类型化输入输出、鉴权、SSE |
| 编排 | LangGraph；按需使用 LangChain 模型适配器 | 单 Agent、有界状态图，不叠加多套 Agent 框架 |
| 数据 | PostgreSQL + pgvector、SQLAlchemy、Alembic | 用户、ACL、项目、文档块、向量、任务、会话 |
| 文档 | Markdown/TXT 解析、pypdf | 文本型 PDF，保存实际页码和段落定位 |
| 检索 | 中文分词 + BM25、向量检索、RRF、可配置 Reranker | 小数据先精确向量搜索，不预设必须 HNSW |
| 模型 | 可替换的生成、Embedding、Reranker 接口 | 不微调；具体模型和资源配置在实施准备阶段锁定 |
| 异步任务 | 同一代码库的 worker + PostgreSQL 任务表/租约 | 执行索引和查询任务，首版不引入 Redis/Celery |
| 交付 | Docker Compose、pytest、前端测试、GitHub Actions | 模拟测试与真实模型评测分别运行 |

pgvector 支持与业务数据共库存储向量及精确/近似查询，因此采用单 PostgreSQL 作为本规模的简化选择，不宣称它在所有规模下最优。[官方说明](https://github.com/pgvector/pgvector)

```text
员工/管理员 Web
        │ 同源会话鉴权 / 管理 API / 查询 API
        ▼
FastAPI ── 权限服务 ── PostgreSQL（业务、ACL、文档、向量、任务、会话）
        │                         ▲
        └── 创建持久查询任务 ── Worker / LangGraph
                                   │
                         四个只读工具 / 模型适配器
                                   │
                         证据核验与权限复查 → 最终答案
```

原文件存本地受控 volume，不放公开静态目录；引用默认查看提取后的授权段落。需要原件下载时另加权限接口，不是 MVP 必需项。

## 7. Agent 执行契约

### 7.1 状态和流程

图状态至少包含：run_id、conversation_id、服务端 auth_context、knowledge_epoch、当前问题、已确认 project_id、澄清候选、证据引用、工具结果、调用预算、失败码、最终答案。

流程：鉴权 → 安全装载会话 → 意图/项目识别 → 必要澄清 → 固定检索或工具循环 → 证据检查 → 权限/版本复查 → 回答。

- 明确的制度问答使用固定检索路径；跨项目状态、人员、文档的问题使用工具循环。
- 不确定路由时优先澄清或进入受限查询，不能直接凭模型常识给出企业事实。
- LangChain 官方区分固定两步检索与模型动态选择工具的 Agentic RAG；本文使用这一职责划分，不要求每个请求都经过完整工具循环。[官方架构说明](https://docs.langchain.com/oss/python/deepagents/retrieval)

### 7.2 执行上限（初始配置，非实测表现）

- 每轮最多 6 次工具执行尝试（包括重试）、5 次生成模型调用（包括最终回答）。
- 对相同工具和相同参数最多执行 2 次；检索改写最多 1 次；可重试超时/暂时不可用最多重试 1 次。
- 无权限、参数非法、确定性无结果不重试。
- 工具单次默认 5 秒超时，生成模型单次 20 秒，整轮 60 秒截止；任何单次调用不得越过剩余总时间。
- 超预算必须停止：已有可靠证据返回 partial，否则返回 failed/no_answer；不能在后台无限重试。
- worker 恢复后继承原始计数与 deadline，不重置预算。

### 7.3 四个工具

所有输入采用严格 Schema，禁止额外字段；user_id、role、ACL 不由模型提供，由服务端注入。

| 工具 | 业务输入 | 输出 |
| --- | --- | --- |
| search_documents | query；可选 project_id；mode=current/history；history 可选 as_of；top_k=1..10 | 文档/版本/片段 ID、允许显示的标题、文本、定位、有效期、来源类型 |
| get_project_status | project_ref（ID 或名称/别名） | 唯一项目的阶段、健康状态、日期、阻塞、更新时间；或授权候选列表 |
| get_project_owner | project_ref；可选 role | 唯一项目的负责人及角色、更新时间；或授权候选列表 |
| get_document_excerpt | document_id、version_id、chunk_id | 经权限和归属检查的原文段落及允许范围内的相邻段落 |

project_ref 唯一匹配时返回规范 project_id；多个可见匹配返回 NEED_CLARIFICATION（最多 5 个），不先挑一个继续查。get_document_excerpt 只能读取本轮已检索到的版本/片段；更换文档需先检索。

统一工具结果：status、data、evidence_ids、observed_at、error_code、retryable。错误码至少包含 NEED_CLARIFICATION、NOT_FOUND_OR_FORBIDDEN、INVALID_ARGUMENT、TIMEOUT、UNAVAILABLE、CONTEXT_CHANGED。

## 8. 数据模型与知识生命周期

### 8.1 最小逻辑数据模型

| 表/实体 | 核心字段与约束 |
| --- | --- |
| users / groups / user_groups | 用户、角色、禁用状态、用户组成员；密码只保存哈希 |
| auth_sessions | 哈希后的会话令牌、用户、过期时间；登出/禁用可撤销 |
| projects | id、code（唯一）、name、aliases、phase、health、三类日期、blockers、updated_at、revision |
| project_people | project_id、display_name、role、可选 employee_id；不提供无关个人信息 |
| acl_entries | resource_type/id、subject_type/id、read；默认拒绝，组织内可见单独显式标记 |
| documents | id、可选 project_id、title、type、deleted_at、revision |
| document_versions | id、document_id、version_label、content_hash、有效区间、原文路径、处理/发布状态、解析/模型版本 |
| chunks | id、version_id、ordinal、text、page/section、offset、tokens、embedding；唯一(version_id, ordinal) |
| system_state | knowledge_epoch，权限和知识生命周期变更时事务性递增 |
| jobs | 索引任务、状态、attempts、lease_until、错误码、输入版本 ID |
| conversations / messages | owner_id、epoch、消息、答案状态、来源 ID；历史返回前复查 epoch |
| runs / run_events | conversation_id、request_id、幂等键、状态、计数、deadline、事件序号、epoch、终态答案 |
| checkpoints | LangGraph 持久状态；用户/任务绑定，仅服务端可读，不提供原始 dump API |
| feedback / audit_events | 回答 ID、反馈类型、处理状态；管理操作与访问拒绝记录 |

这是逻辑模型，实施可合理合并表，但不得消除唯一性、权限或版本约束。时间存 UTC，界面使用 Asia/Shanghai；测试传入固定时钟。

### 8.2 文档处理与发布

处理状态：uploaded → parsing → indexing → ready，任一步失败进入 failed。发布状态独立：draft → published → archived；删除为文档级 tombstone。

1. 上传只创建新版本草稿，不影响已有发布版本。
2. worker 校验并解析文件，按标题/段落切块；初始目标约 600 中文字符、重叠约 100 字符，保留定位，后续通过开发集调整。
3. 计算真实语义向量并保存；Embedding 模型/维度变更必须建新索引版本，不允许混用旧向量。
4. 所有块就绪后标记 ready，由管理员发布。发布以事务更新有效区间与 epoch，不能让半成品进入查询。
5. 同一逻辑文档的 published 版本有效区间为 [effective_from, effective_to)，不允许重叠；上传时间不决定有效性。
6. current 查询只查指定日期有效版本；history 查询显式允许检索历史版本，并在答案注明历史性质。
7. 新版本失败保留旧有效版本；重复提交同版本处理任务不重复插入块。原始索引任务使用租约恢复，发布使用 document revision 乐观锁避免并发覆盖。
8. 删除事务立即 tombstone 并递增 epoch；任何新查询和引用均不可读。worker 后续清除原文件、块和向量，默认 24 小时内完成，并显示清理失败状态。
9. 过期会话及检查点默认 7 天清理；文档删除后历史内容立即逻辑不可用，但物理清理存在上述保留窗口。此规则不代表撤回客户端或模型供应商已接收的数据。

### 8.3 检索与证据约束

- 在授权且版本有效的集合内分别进行 BM25、向量召回，初始各取 20，再 RRF 融合；重排前候选最多 20，最终上下文最多 6 段，均受模型 token 预算限制。
- 小数据集 BM25 可在进程内维护分词/倒排结构，但权限集合和 epoch 必须参与查询；不能跨权限复用答案或重排结果。
- 每条事实性陈述对应一个或多个 evidence_id；文档引用必须来自本轮工具返回集合，项目事实绑定读取时间与 revision。
- 程序校验引用存在、归属、权限和版本；语义上是否真正支持结论，通过保留集人工核验，不能把“引用 ID 合法”说成“答案必然正确”。
- 当前项目记录是登记状态的来源，不保证现实状态永远最新；历史文档不能覆盖当前记录。冲突时并列来源和时间，明确待确认。

## 9. API、答案与事件契约

### 9.1 API 边界

| API | 用途与授权 |
| --- | --- |
| POST /api/auth/login；POST /api/auth/logout；GET /api/me | 账号会话，登录限流 |
| GET/POST /api/conversations | 本人会话列表/创建 |
| GET /api/conversations/{id}/messages | 本人会话；旧 epoch 返回占位，不返回正文 |
| POST /api/conversations/{id}/queries | 提交 message、client_request_id，返回 run_id（202） |
| GET /api/runs/{id} | 本人查询状态及最终答案 |
| GET /api/runs/{id}/events | 本人 SSE 事件流，支持 Last-Event-ID |
| POST /api/runs/{id}/resume | 本人显式恢复中断任务；检查 epoch、期限与预算 |
| GET /api/documents/{id}/versions/{version_id}/chunks/{chunk_id} | 按资源权限查看原文；再次检查版本归属 |
| POST /api/answers/{id}/feedback | 对本人当前可见回答提交反馈 |
| GET/POST/PATCH /api/admin/projects[/{id}] | 管理员维护项目和人员角色 |
| GET/POST /api/admin/documents；POST /api/admin/documents/{id}/versions | 管理员文档列表、上传、新版本 |
| POST /api/admin/versions/{id}/publish；POST /api/admin/versions/{id}/retry | 管理员发布/重试 |
| DELETE /api/admin/documents/{id} | 管理员逻辑删除 |
| GET/PUT /api/admin/resources/{type}/{id}/acl | 管理员查看/更新可见范围 |
| GET /api/admin/users；GET /api/admin/groups；PUT /api/admin/groups/{id}/members | 预置用户管理范围内的组成员维护 |
| GET /api/admin/jobs；GET/PATCH /api/admin/feedback[/{id}] | 索引进度与反馈处理 |

表内集合/单项简写在实现时拆成明确 OpenAPI 路由。全局字段长度、分页上限、错误码通过 Pydantic 定义；对无权查看的员工资源统一 404，管理角色不足返回 403。

### 9.2 最终答案结构

```json
{
  "run_id": "run-demo-001",
  "status": "answered",
  "answer": "北辰项目当前登记为测试阶段，由李工负责。[E1][E2]",
  "facts": [
    {"text": "当前登记阶段为测试", "evidence_ids": ["E1"]},
    {"text": "负责人为李工", "evidence_ids": ["E2"]}
  ],
  "evidence": [
    {"id": "E1", "type": "project_status", "project_id": "p-demo-001", "revision": 3, "observed_at": "2026-09-11T10:00:00Z"},
    {"id": "E2", "type": "project_owner", "project_id": "p-demo-001", "revision": 3, "observed_at": "2026-09-11T10:00:00Z"}
  ],
  "warnings": [],
  "clarification": null
}
```

status 枚举：answered、needs_clarification、partial、no_answer、failed。文档证据额外含 document_id、version_id、chunk_id、title、locator；warnings 包含 stale、conflict、source_unavailable 等。示例姓名、状态和日期均为合成示例。

### 9.3 任务状态、幂等与恢复

- 任务状态：queued → running → completed / failed；worker 租约丢失变 interrupted；resume 校验通过后回 queued。clarification/no_answer 属于正常 completed，具体看答案 status。
- 每个会话最多一个非终态任务；并发新提问返回 409。唯一(owner_id, conversation_id, client_request_id) 防止刷新重复创建；同键不同请求体返回 409。
- 事件至少有 queued、stage_started、stage_completed、needs_clarification、final、error；每个事件带 run_id 和递增 event_id。
- SSE 断线不取消任务；重连先重新鉴权，再按 event_id 回放允许显示的事件。断线期间权限变更时不回放旧结果。
- 模型答案不做未经核验的逐 token 输出；最终校验通过后一次发送答案，减少权限变更时的部分泄漏风险。
- worker 检查点恢复可能重复一次只读工具或模型调用，允许计入成本；不能重复创建用户消息或追加第二份最终答案。
- 恢复时重新查询动态业务数据；旧证据不能直接当当前事实。超过 deadline 或 epoch 改变则明确失败，用户以新请求重新查询。
- LangGraph 的内存检查点不能跨进程重启保存，因此使用 PostgreSQL 持久化检查点；框架持久化不自动等于完整业务恢复，仍需上述任务状态与权限规则。[官方持久化说明](https://docs.langchain.com/oss/python/langgraph/persistence)

## 10. 评测、验收与交付定义

### 10.1 评测数据与基线

准备 120 条有人工参考答案/证据的问题：80 条开发集、40 条保留测试集。按项目/文档主题分组切分，避免同一问题仅换措辞跨集合；覆盖负责人、状态、文档、历史、跨源、澄清、无答案和冲突。

另建至少 32 个确定性的安全/工程场景，不计入上述语义正确率：越权、权限撤销、文档删除、版本失败、注入、超时、预算耗尽、断线和重启恢复等。

必须比较：

- B0：授权关键词搜索，评价文档检索；没有答案生成，不与回答正确率混算。
- B1：固定文档 RAG，作为文档子集基线。
- B2：固定的“项目数据 + 文档”编排，与 Agent 使用相同权限、工具和生成模型，避免故意给基线少数据。
- B3：本项目的动态查询 Agent。

对 B2/B3 使用同一套跨源任务衡量是否真的需要动态选择工具；至少运行 3 次，公开波动和失败案例，不只挑最好结果。人工查找耗时仅在有实际参与者与计时记录时报告；没有用户试验就不写“节省员工工时”。

### 10.2 拟定质量门槛

以下沿用原提案的质量门槛，作为 v0.2 开发目标；调整必须记录原因、版本及影响，不能因为测试不达标而不留记录地降低。

| 指标 | 定义 | 门槛/报告要求 |
| --- | --- | --- |
| 任务成功率 | 必需事实正确、证据齐全、该澄清/拒答时行为正确的任务数 / 全部任务数 | 保留集三次汇总 ≥85%，每次与各类别结果均公开 |
| 引用支持率 | 人工判断被引用证据确实支持的事实条目 / 所有带引用事实条目 | ≥95%；无依据事实仍使对应任务失败 |
| 证据覆盖率 | 有合格引用的可核验事实条目 / 所有可核验事实条目 | ≥95%；不能靠减少引用数量提高支持率 |
| 检索 Recall@5 | 单题前 5 个结果覆盖的标注相关文档数 / 该题标注相关文档数，再按题平均 | 文档子集 ≥85%，明确按文档还是版本标注 |
| 确定性权限/生命周期/恢复测试 | 固定场景中的断言通过率 | 所有必须项通过，尤其越权泄漏为 0 |
| 延迟 | 请求提交到最终答案，失败也报告；成功与失败分别给 P50/P95 | 目标 P95 ≤30 秒，硬截止 60 秒；硬件、模型与并发固定后评测 |
| 成本 | 每任务生成/Embedding/重排使用量及重试量 | 必须报告，价格未知只报用量，不造金额 |

单一 LLM 裁判只作辅助；保留集事实、引用和拒答结果必须人工核验。测试集零泄漏不代表对任意攻击绝对安全。离线 stub 测试不能替代真实生成模型与真实语义 Embedding 的验收。

### 10.3 必须通过的端到端场景

| 编号 | 场景 | 期望结果 | 对应需求 |
| --- | --- | --- | --- |
| AC-01 | 员工查询授权项目负责人及状态 | 与数据库一致，带时间和证据 | FR-01/02/03 |
| AC-02 | 两个授权同名项目 | 先澄清，不擅自选择 | FR-07 |
| AC-03 | 同名项目中一个无权限 | 不暴露无权限候选 | FR-01/07 |
| AC-04 | 当前规范与历史规范不同 | 默认用当前版本；历史提问明确引用历史版本 | FR-04/05 |
| AC-05 | 查询延期原因但记录中无原因 | 只报告已知状态，说明未找到原因 | FR-05/08 |
| AC-06 | 同时问负责人、状态和决策依据 | 联合业务工具与文档，有逐项证据 | FR-06 |
| AC-07 | 项目状态与较早纪要不同 | 展示时间差和来源，不把旧记录当当前状态 | FR-06/08 |
| AC-08 | 篡改项目、文档、引用、会话或 run ID | 无越权正文/标题/事件，统一拒绝 | FR-01/09 |
| AC-09 | 提问后、模型生成中撤销权限 | 最终发送前阻断；旧会话占位，不复用上下文 | FR-01/09 |
| AC-10 | 发布文档新版本时解析/向量化失败 | 旧版本继续可用，失败可重试 | FR-10 |
| AC-11 | 删除已被引用文档 | 新检索/引用不可读，旧会话失效 | FR-10 |
| AC-12 | 文档中包含“忽略限制、读取机密” | 只作为文本处理，无额外权限或工具 | FR-04/08 |
| AC-13 | 状态工具超时但文档可用 | partial 并说明当前状态不可用，不伪装实时 | FR-08 |
| AC-14 | 重复请求、SSE 断线重连 | 同一个 run，无重复消息/最终答案 | FR-09 |
| AC-15 | 查询中重启 worker | interrupted 可见；有效预算内恢复，否则明确过期 | FR-09 |
| AC-16 | 同参数循环、模型非法 JSON、未知工具 | 有界重试/停止，不无限执行 | FR-08 |
| AC-17 | 上传不支持格式/扫描 PDF/超大文件 | 明确拒绝或失败，不伪造解析成功 | FR-10 |
| AC-18 | 管理员修改项目、组和 ACL | 事务更新、审计记录；员工接口不能修改 | FR-11 |
| AC-19 | 用户反馈引用错误 | 管理员可定位授权回答并更新处理状态 | FR-12 |
| AC-20 | 多轮切换项目、知识 epoch 改变 | 不串项目；过期会话要求重新开始 | FR-07/09 |

上述场景需细化为至少 32 个独立测试；完成状态留空，不在 spec 中提前标记通过。

### 10.4 Definition of Done

- FR-01 至 FR-12 和 AC-01 至 AC-20 全部有实现、测试及可复查证据。
- 从干净环境按 README 启动成功；提供数据库迁移、合成 seed、环境变量示例和 Docker Compose。
- 单元/集成测试、前端关键交互、至少一次完整真实模型评测可复现；CI 不依赖私人企业数据。
- 交付架构与权限说明、OpenAPI、评测数据及报告、失败案例、3 分钟演示脚本、依赖许可证说明。
- 没有已知越权漏洞或阻断核心流程的缺陷；非阻断问题列入已知限制。
- 本人能解释四个工具、检索选择、权限与版本策略、动态 Agent 相比固定流程的实际结果。
- 代码完成不自动等于真实企业价值已验证或必然获得面试；这两类结论单独记录。

## 11. 实施顺序与范围控制

| 阶段 | 产物 | 退出条件 |
| --- | --- | --- |
| M0：开发准备 | 建立本文、确认本机环境、选择模型/资源、准备数据和评测标注 | 核心接口可用，真实模型阶段的资源配置有记录；不阻塞先实现身份与数据 |
| M1：身份与数据 | 用户、ACL、项目查询、管理员表单、种子数据 | 不经过 LLM 也能验证全部权限规则 |
| M2：知识查询 | 文档处理、版本发布、关键词/向量检索、固定 RAG | 文档问答、删除和版本失败测试通过 |
| M3：Agent 与会话 | 四工具、跨源回答、澄清、检查点、事件流 | 核心演示和中断恢复完成 |
| M4：验收交付 | 对抗测试、保留集评测、基线比较、README 和演示 | 满足第 10 节 DoD |

原方案的 65–95 有效小时仅为讨论估算；本 spec 细化了权限撤销和恢复约束，需在 M0 后重新拆分估算，不直接换算成保证交付日期。

若工期超出预算，优先削减界面美化、管理员批量导入和自动调参；不得削减鉴权、引用真实性、版本一致性、失败提示和真实模型评测。任何范围变更先更新 spec。

## 12. 已作默认选择与实施前待定事项

### 已作默认选择

- Web 独立演示，不接飞书；业务查询是本地受控数据服务，不冒充真实企业接口。
- 单 Agent、四个只读工具，简单问题保留固定路径。
- 单 PostgreSQL，后台 worker，无额外消息队列或独立向量数据库。
- 数据管理员全库管理，员工最小权限；epoch 变更使旧会话失效。
- 不做答案缓存，不流式输出未校验的答案正文。

### 待落实事项及处理方式

1. 生成/Embedding/Reranker 的具体模型、API 供应商、预算上限及本地硬件条件。
2. 合成资料为主，还是另有可公开/授权的真实资料；是否能邀请实际使用者测试。
3. 实施日程与模型阶段所需资源；质量门槛沿用第 10 节，依赖版本在兼容性 smoke test 后锁定。

默认采用合成资料，按第 14–15 节先推进可独立完成的本地开发。模型提供方、模型 ID、Embedding 维度和上下文限制通过配置注入；尚未接入时明确显示模型未配置，不用伪答案冒充真实效果。上述未知项不构成本次文档交付或 M1 的阻塞，也不得被解释为允许上传企业机密、购买服务、公开部署或开始投递。

## 13. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.1（历史提案） | 2026-09-11 | 形成 B 精简版产品与技术规格；原文保留在 learning-path/proposals/ |
| v0.2（本文） | 2026-09-11 | 在 project/ 建立开发基线；沿用 FR/AC 编号，补充代码组织、阶段任务、32 项工程场景及实现细则；未标记任何实现或测试完成 |
## 14. 工程组织与模块职责

### 14.1 目标目录

下列是后续实现的目标布局，当前仅创建本文，不表示这些文件已存在。所有路径相对 `project/`。

```text
project/
├── PRODUCT_TECH_SPEC.md       # 产品范围、技术契约、验收基线
├── README.md                 # 本地启动、演示账号、评测与限制
├── .env.example              # 配置名及占位值，不含真实密钥
├── compose.yaml              # web、api、worker、PostgreSQL
├── backend/
│   ├── pyproject.toml
│   ├── alembic/              # 迁移、约束与索引
│   ├── src/knowledge_agent/
│   │   ├── api/              # HTTP 路由、Schema、错误映射、SSE
│   │   ├── auth/             # 会话、CSRF、用户与统一权限服务
│   │   ├── db/               # 连接、模型、事务
│   │   ├── projects/         # 项目消歧、状态和人员查询
│   │   ├── documents/        # 上传、解析、切块、版本、删除
│   │   ├── retrieval/        # 授权过滤、BM25、向量、RRF、重排
│   │   ├── agent/            # 图状态、路由、四工具、预算、核验
│   │   ├── models/           # 生成、Embedding、Reranker 适配器
│   │   ├── conversations/    # 消息、run、幂等、事件与恢复
│   │   ├── worker/           # 任务领取、租约、索引、查询、清理
│   │   └── observability/    # 脱敏日志、耗时、用量、审计
│   └── tests/                # 单元、数据库集成、安全与恢复测试
├── frontend/
│   └── src/                  # 登录、员工查询、管理数据、管理反馈
├── data/synthetic/           # 明确标注合成的数据与 seed 生成规则
├── evaluation/               # 问题、标注、基线、运行配置、结果
└── docs/                     # 架构细化、演示脚本、许可证、失败案例
```

API 只校验请求、鉴权并调用业务模块，不在路由内堆叠 Agent 逻辑。工具复用项目和检索服务；后台表单调用管理服务。所有数据库访问通过服务端权限上下文，不由提示词决定授权。

### 14.2 运行环境与配置

- 本地 Python 开发沿用仓库学习环境 `hello_agents_py311`（Python 3.11）；容器运行环境单独锁定。本次未运行环境验证，不声称依赖已经可用。
- 前端 Node、包管理器和 Python 依赖在首次安装及兼容性验证后锁定；不以浮动 latest 作为可复现环境。
- 同一套后端源码提供 API 和 worker 两个进程入口。数据库迁移先于进程启动；容器健康检查区分存活和依赖就绪。
- PostgreSQL 保存持久状态，原件保存到受控 volume；不将原件、生产数据、检查点或密钥提交到代码仓库。
- `.env.example` 至少列出数据库连接、文件目录、Cookie/Origin、模型提供方/模型 ID、API Key 占位、Embedding 维度、Reranker 开关和执行预算。启动时校验必需配置和维度一致性。
- 模型适配器统一返回调用结果、耗时、模型标识和实际用量；供应商未返回用量时标记 unknown，不填 0。Reranker 可关闭；Embedding 的真实语义能力是最终验收要求。
- stub 仅用于明确标记的开发/测试模式。真实模型评测独立运行并记录完整配置，不把 stub 结果计入语义指标。

### 14.3 关键实现细则

**时间与有效版本。** 请求采用服务端时钟。`current` 的有效性基准是本轮查询时刻，不接受模型伪造“当前时间”；`history/as_of` 使用严格日期格式。存在多个历史版本时保留版本与时间差异，不将其拼成一个无时间边界的结论。即使 epoch 没变，检索和引用也要重新判断有效区间，覆盖预先设定的生效/失效时间边界。

**业务事实更新。** 状态、日期、阻塞或人员角色变更，必须在同一事务内递增项目 revision 并更新 updated_at；关联人员表的修改也遵守此规则。最终答案前复查本轮项目证据 revision，发生变化则返回 CONTEXT_CHANGED，用户重新提问。普通项目数据更新不递增全局 epoch；已完成回答保留其读取时刻，新一轮重新读取项目事实。

**授权与事件读取。** 查询服务和所有读取入口共享权限谓词。证据面板不能直接访问文件路径或公开静态 URL。会话、run、事件和引用接口均在读取时检查当前身份、所有权和 epoch；旧答案不通过事件回放或反馈界面绕过失效策略。反馈页仅展示该条反馈、必要的回答及来源，不返回对应私人会话；回答失效时保留 ID 和状态占位。

**任务互斥。** 数据库保证一个会话最多一个非终态 run，非终态包括 queued、running、interrupted。中断任务可恢复；过 deadline 后由清理任务或下一次提交前的事务将其置为 failed，再允许新任务。创建用户消息与 run 在同一事务完成；终态答案以 run_id 唯一约束避免重复落库。重复幂等键先返回既有结果；只有同键不同请求内容或另一个有效任务仍在进行时返回 409。

**租约和副作用。** worker 按租约领取任务，并使用领取代次校验后续写入；失去租约的旧 worker 不得提交阶段事件或最终答案。每次调用前持久记录尝试次数和剩余预算，恢复时不重新获得完整预算。允许外部只读请求被重复执行，不允许因此生成重复用户消息、文档块或最终回答。

**对话澄清。** 需要澄清时结束当前 run 并持久保存授权候选 ID；用户下一轮选择后仍需验证候选、epoch 和项目 ACL。直接输入另一个项目时重新消歧，不将上轮项目自动覆盖当前明确指代。模型内部推理不持久展示为“查询进度”。

**证据与降级。** 回答按事实条目绑定证据，渲染正文中的引用必须能解析到同一证据集合。任何不可核验的业务事实不能作为已知结论输出。合法但无结果为 no_answer；有部分可支持结论且部分来源失败为 partial；系统错误且无可靠证据为 failed。权限/epoch 变化一律阻断旧结果，不以 partial 返回受影响内容。

## 15. 分阶段实施任务

阶段退出条件用于判断完成程度，不是逐次向用户请求批准。任务按依赖推进；涉及真实模型的资源待定，不阻塞身份、业务数据、管理界面和确定性测试。以下任务均尚未完成。

| 顺序 | 任务与交付 | 依赖 | 完成证据 |
| --- | --- | --- | --- |
| T01 | 后端/前端骨架、配置、PostgreSQL、迁移、健康检查 | 本文 | 干净环境启动与迁移记录 |
| T02 | 账号 seed、登录登出、角色、组、ACL、epoch | T01 | 身份与权限集成测试 |
| T03 | 项目及人员 CRUD、两个只读业务工具、管理表单 | T02 | AC-01/02/03/18 的业务与权限断言 |
| T04 | 文档上传、受限解析、切块、任务租约与重试 | T02 | 格式限制、解析失败、重复索引测试 |
| T05 | 版本发布、有效区间、删除、引用查看 | T04 | AC-04/10/11/17 |
| T06 | 授权 BM25/向量召回、RRF、可选重排、固定 RAG | T05、真实模型配置 | 检索结果、引用与真实 Embedding 验证 |
| T07 | 单 Agent、四工具、消歧、跨源答案、预算及核验 | T03/T06 | AC-05/06/07/12/13/16 |
| T08 | 会话、幂等 run、持久检查点、SSE、恢复 | T02/T07 | AC-08/09/14/15/20 |
| T09 | 员工完整查询页、证据面板、反馈与管理处理 | T05/T08 | 关键前端交互和 AC-19 |
| T10 | 完整合成数据、120 题标注、32 项工程场景 | T03 起分步准备 | 数据统计、切分说明、参考证据 |
| T11 | B0–B3、保留集三次运行、成本延迟、失败分析 | T06–T10 | 实际评测报告与全部原始运行标识 |
| T12 | README、Compose、CI、许可证、演示、已知限制 | T01–T11 | 第 10.4 节 DoD 逐项证据 |

每项实现后更新实际结果和未解决项。仅完成 T01–T03 属于业务与权限底座，不能宣称整个 Agent 项目完成。功能实现、确定性测试通过、真实模型评测达标分开记录。

## 16. 确定性工程测试清单

下列 32 项是第 10 节的最小拆分，初始状态全部为待实现/待验证。语义质量仍需独立保留集人工核验；此表不能替代真实模型评测。

| 编号 | 场景与关键断言 | 关联验收 |
| --- | --- | --- |
| ET-01 | 篡改请求中的用户/角色/组字段不能切换身份 | AC-01/08 |
| ET-02 | 员工不能调用管理写接口，数据库无修改 | AC-18 |
| ET-03 | 未授权项目与不存在项目返回一致的拒绝结果 | AC-03/08 |
| ET-04 | 关联文档必须同时满足文档 ACL 与项目 ACL | AC-08 |
| ET-05 | 两个可见同名项目返回澄清，不能自行查询其一 | AC-02 |
| ET-06 | 不可见同名项目不出现在候选或数量提示中 | AC-03 |
| ET-07 | 篡改其他用户 conversation/run ID 不返回正文或事件 | AC-08 |
| ET-08 | 混用 document/version/chunk ID 或工具读取未召回片段被拒绝 | AC-08 |
| ET-09 | 授权过滤发生在召回/重排上下文构造前，模型输入无受限正文 | AC-08/12 |
| ET-10 | 模型生成期间撤销权限，最终答案阻断且旧会话隐藏 | AC-09 |
| ET-11 | 组成员移除后旧上下文不再发给模型 | AC-09/20 |
| ET-12 | epoch 变化后重连 SSE 不回放旧 final，run 查询不返回旧答案 | AC-09/14 |
| ET-13 | 同文档版本区间不可重叠，并发发布由 revision 防覆盖 | AC-04/10 |
| ET-14 | 生效边界的 current/history 选择正确，引用保留历史标识 | AC-04 |
| ET-15 | 新版本解析失败时旧发布版本仍可查询 | AC-10 |
| ET-16 | Embedding 失败或维度不符不得发布，旧版本仍可查询 | AC-10 |
| ET-17 | 同一版本重复索引及失败后重试不产生重复块 | AC-10 |
| ET-18 | 删除后检索、原文、引用立即不可读，历史会话失效 | AC-11 |
| ET-19 | 清理任务失败可见且可重试，不恢复逻辑可见性 | AC-11 |
| ET-20 | 不支持格式、超大文件、超页数、扫描 PDF 有明确失败 | AC-17 |
| ET-21 | 文档注入不能增加工具、修改身份或访问任意路径/URL | AC-12 |
| ET-22 | 状态工具超时且文档可用时仅返回有依据的 partial | AC-13 |
| ET-23 | 未记录延期原因时明确缺失，不从日期推断业务原因 | AC-05 |
| ET-24 | 旧纪要不覆盖当前登记状态，答案保留各自时间和来源 | AC-07 |
| ET-25 | 重复提交同一幂等键仅创建一条用户消息及一个 run | AC-14 |
| ET-26 | 同键不同请求体或同会话并发新任务返回 409 | AC-14 |
| ET-27 | SSE 按事件序号重连，页面不重复追加最终答案 | AC-14 |
| ET-28 | worker 中断恢复继承预算、重读当前数据，旧 worker 不能写回 | AC-15 |
| ET-29 | deadline 过期或 epoch 改变不能恢复旧任务，状态明确失败 | AC-15/20 |
| ET-30 | 重复参数、非法 JSON、未知工具、暂时失败均受次数/时限约束 | AC-16 |
| ET-31 | 多轮切换项目与澄清选择不会串项目，跨源事实各有证据 | AC-06/20 |
| ET-32 | 仅本人可反馈当前可见回答，管理员处理不暴露私人会话 | AC-19 |

每个表项可包含多个参数化用例。还需为密码/会话、CSRF、上传路径、HTML 转义、审计脱敏、项目 revision 变化等编写对应安全与业务测试；32 项是下限，不是排除其他必须项的上限。

## 17. 当前交付状态

- 已交付：本产品技术文档，包含产品范围、界面、权限、数据与接口契约、实施任务和验收清单。
- 尚未交付：应用代码、数据库迁移、合成数据集、测试、启动验证、真实模型接入与评测报告。
- 后续第一步：按 T01–T03 建立工程骨架、身份权限与项目查询，再推进知识处理和 Agent。
- 本文使用原提案的技术选型作为设计约定；具体依赖版本、第三方接口兼容性和运行表现须在实现时验证，不作为已验证事实。
