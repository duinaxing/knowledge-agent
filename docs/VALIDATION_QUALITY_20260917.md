# 质量评测：2026-09-17

结论：开发集 **50 个案例、55 次问答**完成真实登录、会话、任务、SSE、引用和历史持久化链路检查，无执行错误或安全停止。生成模型为模拟网关，**不能宣称真实回答质量验收通过**。独立人工审阅覆盖率为 0，任务正确率、引用事实支持率及正确拒答率保持空值。

## 可复现输入

冻结题库 `evaluation/quality-v1.json` 共 150 例，开发集 50、保留集 100，按主题分开；指定文档、未点名、简称、比较、追问、历史版本、证据不足、权限、通用问答和文档提示注入各 15 例。全量包括 165 次问答。语料 60 份文档、75 个版本；Markdown 正文通过实际解析、Embedding、索引和发布流程初始化。

- 数据集 SHA-256：`2568c610fe517c4b9457df4715fc0e517a7fa42a555afe1cf9b722cc95135b5f`
- 语料 SHA-256：`a99adc02cda45c05321fff04f32c20dec1974a5816f7c394456b6e2bdd6e9284`
- Embedding：本地 `BAAI/bge-small-zh-v1.5`，512 维；分块 600 字符，重叠 100。
- 隔离数据库：`knowledge_quality_test`；原始输出 `runtime/quality/results/20260917-105334-9213df`。
- 基线代码 `fb1e745` 加本次工作区改动，manifest 明确记录 dirty 状态；后续启动器另保存逐源文件哈希，不能将其追溯声称为本轮启动时已有的记录。

## 自动观察结果

| 分类 | 案例数 | 目标证据 Recall@6 |
|---|---:|---:|
| 指定文档 | 5 | 100% |
| 未点名文档 | 5 | 100% |
| 简称 | 5 | 100% |
| 跨文档比较 | 5 | 100% |
| 多轮追问 | 5 | 60% |
| 历史版本 | 5 | 90% |
| 文档提示注入 | 5 | 100% |
| 证据不足、权限、通用 | 各 5 | 无目标证据，不纳入 Recall |

可回答题按案例平均 Recall@6 为 **92.86%**。它只反映当前模拟规划器产生的检索请求及真实检索结果，不能推断 DeepSeek 的查询改写或答案质量。引用片段 ID、版本、正文和可访问性核验通过；跨用户会话读取、私有文档读取均为 404；重新登录后历史仍可读取。50 例分流类型与预期一致，但拒答是否语义恰当仍待人工核验。

漏检保留在 JSONL 中：`q00-followup`、`q02-followup` 未命中目标片段；`q00-version` 仅命中两个目标版本中的一个。模拟规划器直接使用追问文本，没有真实模型的语义消歧；历史检索还受到候选排序影响。不能仅为了提高模拟分数就调整线上分块或重排。真实开发集应先检查查询改写与版本上下文，再决定是否调整检索。

模拟回答会从可见候选中提取固定校验码，可能包含无关主题；数字字符串命中也不能证明支持天数正确。这正是保留人工任务正确率和引用支持率、不把自动事实要素匹配当成最终质量的原因。文档注入字符串没有导致本轮越权或输出禁止标记，但这不等于真实模型抗注入验收通过。

## 待完成验收

本轮真实生成模型调用为 0；保留集未运行、未用于调整规则或参数。参考标注本身尚待独立审阅。真实阶段需显式 `-Real`，开发集与保留集使用同一质量预算实验编号，累计最多 300 次问答和 1500 次模型请求，重试计数且重启不清零。

`review.csv` 已生成，空白审阅导入得到 `PENDING`、覆盖率 0，而非自动通过。真实结果由独立审阅者确认参考、事实与引用支持关系后，才能判定任务正确率 ≥90%、拒答率 ≥95%、引用支持率 ≥95%，以及安全错误为零。预算耗尽、未审阅项、失败案例均不得被计为通过。
