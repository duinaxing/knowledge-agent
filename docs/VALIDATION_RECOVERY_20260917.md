# 恢复验证：2026-09-17

结论：本机隔离实例的数据库、原始文件和权限数据恢复验证通过，完整备份、恢复及 HTTP 验证耗时 **17.406 秒**，低于当前测试规模 15 分钟目标。未验证生产规模和跨机器灾难恢复，不承诺生产 RPO。

## 实际演练

- 源：`knowledge_loadtest_100`，停止测试 API 和 worker，取得测试启动器排他锁；数据库无其他连接才开始。
- 目标：`knowledge_restore_test`、`runtime/recovery/restored-files`。目标不存在时创建，拒绝覆盖已有数据库或目录。
- 一致性时间点：2026-09-17 02:53:46 UTC（北京时间 10:53:46）。表级 SHARE 锁覆盖数据库导出和原件复制。
- 备份：`runtime/recovery/backup-20260917-105346`。包含 PostgreSQL 自定义格式导出、原件、逐文件 SHA-256、版本路径映射、迁移版本、代码提交、Embedding 配置和表数量；不包含模型密钥。
- 本轮 HTTP 检索复用质量测试的 `127.0.0.1:8101` Embedding 服务，生成模型调用为零。

恢复前后比对已完成回答及其会话归属、文档删除状态、版本有效区间、用户组成员、ACL 和片段内容的稳定摘要。文件路径重映射到恢复目录，所有登录会话作废。重新登录后验证历史读取、真实向量检索、引用片段、无权文档 404，并使用仅在恢复目标创建的测试管理员遍历文档版本内容接口。

为验证中断处理，在停止服务的测试源中暂存一条中断任务和一条已删除文档，备份结束后从测试源移除。恢复后的中断任务为 `failed / RESTORED_REQUIRES_RETRY`，执行代次从 2 增至 3；删除标记保留。没有启动恢复目标 worker，也没有自动重放模型请求。

## 反例检查

基于实际备份复制出的隔离副本执行以下检查，全部得到预期拒绝：

| 输入 | 实际结果 |
|---|---|
| 完整原备份 | 哈希核验通过 |
| 删除数据库导出文件 | 明确拒绝：备份文件缺失 |
| 修改数据库导出字节 | 明确拒绝：校验失败 |
| 恢复目标为业务库名 | 拒绝非白名单目标 |
| 恢复目标已经存在 | 拒绝覆盖，已恢复实例保留 |

现场报告 `runtime/recovery/report.json`、`REPORT.md` 与 `negative-checks.json` 保留在本地，运行库和文件不进入 Git。已有恢复目标下再次执行入口会拒绝；需要保留、人工检查或另行明确清理目标后才能做第二次完整演练。
